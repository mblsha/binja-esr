Rust/Python overlay parity plan
- Done: MemoryImage overlay support with handler/data backing, PC-aware overlay read/write logs, RuntimeBus PC propagation, and parity unit tests for handler reads, data writes, and fallback behavior.
- Done: Convenience helpers mirroring Python overlays (`add_ram_overlay`, `add_rom_overlay`, `load_memory_card`) and overlay name tagging in Perfetto writes.
